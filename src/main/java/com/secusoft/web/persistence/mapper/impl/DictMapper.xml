<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.secusoft.web.persistence.mapper.DictMapper">

	<!-- 通用查询映射结果 -->
	<resultMap id="BaseResultMap" type="com.secusoft.web.persistence.model.Dict">
		<id column="id" property="id" />
		<result column="num" property="num" />
		<result column="pid" property="pid" />
		<result column="name" property="name" />
	</resultMap>

	<sql id="Base_Column_List">
		id, num, pid, name
	</sql>

	<select id="selectByPid" resultType="dict">
		select
		<include refid="Base_Column_List" />
		from dict
		where pid = #{pid}
	</select>

	<select id="list" resultType="map">
		select * from dict
		where pid = 0
		<if test="condition != null and condition != ''">
			AND name like CONCAT('%',#{condition},'%')
		</if>
		order by id ASC
	</select>

	<select id="selectByName" resultType="map">
		SELECT
		<include refid="Base_Column_List" />
		FROM dict WHERE pid = (SELECT id FROM dict WHERE name=#{name})
	</select>

</mapper>
