<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.secusoft.web.persistence.mapper.RoleMapper">

	<!-- 通用查询映射结果 -->
	<resultMap id="BaseResultMap" type="com.secusoft.web.persistence.model.Role">
		<id column="id" property="id" />
		<result column="name" property="name" />
	</resultMap>

	<sql id="Base_Column_List">
		id, name
	</sql>

	<select id="selectRoles" resultType="map">
		select
		<include refid="Base_Column_List" />
		from role
		<if test="condition != null">
			where name like CONCAT('%',#{condition},'%')
		</if>
	</select>

	<delete id="deleteRolesById">
		delete from relation where roleid = #{roleId}
	</delete>

	<select id="roleTreeList" resultType="com.secusoft.web.core.node.ZTreeNode">
		select id "id",name as "name",('true') "open" from role
	</select>

	<select id="roleTreeListByRoleId" resultType="com.secusoft.web.core.node.ZTreeNode">
		SELECT
		r.id "id",
		NAME AS "name",
		('true') "open",
		(
		CASE
		WHEN (r1.ID = 0 OR r1.ID IS NULL) THEN
		'false'
		ELSE
		'true'
		END
		) "checked"
		FROM
		role r
		LEFT JOIN (
		SELECT
		ID
		FROM
		role
		WHERE
		ID IN

		<foreach collection="array" index="index" item="i" open="(" separator="," close=")">
			#{i}
		</foreach>

		) r1 ON r.ID = r1.ID
	</select>

</mapper>
